package net.sourceforge.vrapper.plugin.splitEditor.commands;

public enum SplitMode {
    /** Make a clone of the current editor (vim behavior). */
    CLONE,
    /** Move current editor into the new split. */
    MOVE,
}
